---
import BaseLayout from '../layouts/BaseLayout.astro';
import Icon from '../components/Icon.astro';
import { about } from '../data/copy/about';

const title = `${about.headline} — The Plain Office`;
---

<BaseLayout title={title} description={about.intro} path="/about">
  <section class="section" style="background:var(--color-cream-2); text-align:center;">
    <div class="wrap">
      <h1>{about.headline}</h1>
      <p class="lede" style="margin:0 auto; text-align:center;">{about.intro}</p>
    </div>
  </section>

  <section class="section" style="background:var(--color-cream-2);">
    <div class="wrap">
      <div class="photo-frame">
        <div class="photo-placeholder">
          <!-- Placeholder: no photo of a ledger/desk scene was supplied. -->
          Photo — placeholder
        </div>
      </div>

      <div class="notice" style="margin-top:32px; max-width:none;">
        <h3 style="margin:0 0 6px; color:var(--color-navy);">{about.privacyPromise.heading}</h3>
        <p style="margin:0; font-style:italic;">{about.privacyPromise.body}</p>
      </div>
    </div>
  </section>

  <section class="section" style="background:var(--color-cream-2); text-align:center;">
    <div class="wrap">
      <h2 class="no-rule" style="border-bottom:1px solid var(--color-rule-2); padding-bottom:28px;">{about.conventions.heading}</h2>
      <div class="conventions-row" style="margin-top:32px;">
        {about.conventions.items.map((item, i) => (
          <div class={i > 0 ? 'conventions-row-item' : ''}>
            <Icon name={item.icon as any} />
            <h3>{item.title}</h3>
            <p>{item.body}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="section" style="background:var(--color-cream-2);">
    <div class="wrap">
      <hr class="divider-line" style="margin-bottom:40px;" />
      <div class="two-col">
        <div>
          <h3>{about.honestFit.heading}</h3>
          <p style="color:var(--color-ink-secondary); margin:0;">{about.honestFit.body}</p>
        </div>
        <div class="verdict-box">
          <p class="kicker">{about.verdict.kicker}</p>
          <p class="quote">{about.verdict.quote}</p>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
